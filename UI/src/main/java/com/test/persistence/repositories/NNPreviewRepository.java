package com.test.persistence.repositories;

import com.test.persistence.entities.NNPreview;
import org.springframework.data.jpa.repository.JpaRepository;

public interface NNPreviewRepository extends JpaRepository<NNPreview, Long> {
}
